/*Chef has an array AA having NN elements. Chef wants to make all the elements of the array equal by repeating the following move.

Choose any integer KK between 11 and NN (inclusive). Then choose KK distinct indices i_1 , i_2, \dots, i_Ki , and increase the values of A_{i_1} , A_{i_2} ,
\dots , A_{i_K}A K  by 11.
Find the minimum number of moves required to make all the elements of the array equal.
*/

#include <iostream>
#include<bits/stdc++.h>
using namespace std;

int main() {
	// your code goes here
	long int t,n,i,r;
	cin>>t;
	while(t--)
	{
	    cin>>n;
	    long int a[n];
	    for(i=0;i<n;i++)
	    {
	        cin>>a[i];
	    }
	    sort(a,a+n);
	    cout<<(a[n-1]-a[0])<<endl;
	}
	return 0;
}
